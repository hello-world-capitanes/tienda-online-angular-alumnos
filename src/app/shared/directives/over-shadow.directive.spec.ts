import { OverShadowDirective } from './over-shadow.directive';

describe('OverShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new OverShadowDirective();
    expect(directive).toBeTruthy();
  });
});

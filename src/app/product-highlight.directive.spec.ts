import { ProductHighlightDirective } from './product-highlight.directive';

describe('ProductHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
